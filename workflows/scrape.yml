name: 📅 Auto Scrape Beyblade Events

on:
  # 手動トリガーを可能にする
  workflow_dispatch:
  # 毎日UTC 5:00 (日本時間 14:00) に自動実行
  schedule:
    - cron: '0 5 * * *'

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # プッシュアクションのためにトークン権限が必要
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          
      - name: Run Scraper
        run: python scraper.py
        
      - name: Commit and Push if changed
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          # data/events.json のみが変更対象
          file_pattern: 'data/events.json'
          commit_message: "🤖 Feat: Update event data (Automatic Scrape)"
          # GitHub Pagesで使用しているブランチ名に変更してください (例: main, master)
          branch: main
