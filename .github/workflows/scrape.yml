name: 📅 Auto Scrape Beyblade Events

on:
  # 手動実行を可能にする
  workflow_dispatch:
  # 毎日自動実行（UTC 5:00 = 日本時間 14:00）
  schedule:
    - cron: '0 5 * * *'

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # コミット権限を付与
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          
      # Playwrightが依存するブラウザエンジンをインストール
      - name: Install Playwright browsers
        run: playwright install chromium
          
      - name: Run Scraper
        # -u オプションでPythonの標準出力バッファリングを無効化し、ログを強制出力
        run: python -u scraper.py
        
      - name: Commit and Push if changed
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          file_pattern: 'data/events.json'
          commit_message: "🤖 Feat: Update event data (Automatic Scrape)"
          # GitHub Pagesが公開されているブランチ名に合わせてください (例: main, master)
          branch: main
